function calculateSumTriple(a, b) {
    if (a == b) {
        return 3 * (a + b);
    } else {
        return (a + b);
    }
}

console.log(calculateSumTriple(10, 10));
console.log(calculateSumTriple(10, 20));